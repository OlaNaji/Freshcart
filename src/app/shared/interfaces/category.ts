export interface Category {
    _id: string
    name: string
    slug: string
    image: string
  }


  export interface brand {
    _id: string
    name: string
    slug: string
    image: string
    createdAt: string
    updatedAt: string
  }